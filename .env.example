# Gemini API Key (required - for AI-powered chat and report generation)
# Get your API key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here
# Alternative: You can also use API_KEY for backwards compatibility
# API_KEY=your_gemini_api_key_here

# Note: Users can choose between cloud storage (Google login) or local storage (anonymous mode)
# Local storage uses IndexedDB (browser storage) for anonymous users
